# Frontend has no required env vars currently — all config (API base URL, etc.)
# is handled via the Vite dev proxy in vite.config.js.
# This file exists as a placeholder for future frontend-only env vars.
